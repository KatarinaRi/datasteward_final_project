TO DO
- LinkML (and XSD) cannot enforce significant figures natively. There is no built-in constraint for this. It is possibleto document in the description, e.g. "Report to a maximum of 4 significant figures (e.g. 0.04216)". Significant figures are a reporting convention, not really a data type constraint.
- to decide whether we only want European countries, languages or global. So far, only European have been included.
- Handling not_relevant and not_reported values in the schema?
- consider if including NUTS3 as enum, since it is a large list. For now, it has been included as slot. 
But this solution does not enable full validation.
-  AquaticMatrixFraction -  To consider whether this needs to be included as separate enum, or included 
  in the matrix enum.
- Taxons are as slots now - include as enum? Which level?
- chemical grouping - to be discussed
- other placeholders - solve
- fix the encoding issue to be able to use eg. greek mi, superscripts, etc.
- Vocabularies developed separately in PARC - add/reference 
- The schema is in yaml from which it is possible automatically generate 
validation schema (JSON schema), ontology (OWL), SHACL Shapes (rdf validation), 
documentation (MarkDown), python classes and code. 
-> check how this is done... and generate what is appropriate
- decide on whether to map or to use meaning (and adopt external URIs)
- check the adopted URIs - where do they resolve?
- look at vocbaulary analysis by NTK and proposals of vocabularies to be reused. -> any inspiraiton?
- To clarify how uncertainty should be expressed
- make contact part of institution?
- What field to use as site identifier?
- Align contacts, funders and institution, managing instance name with DCAT-AP Agent?
- Indicate explicitly "required false", even if this by default - to make it more clear and transparent.
- Figure out linking biota with compartment. Currently, we have BiotaCompartment and Domain - how to aling and link?
- Consider distinction between "sampled matrix (sample level)" and analysed matrix (measurement/observation level)
- matrix/domain- specific sampling method - solve similarly as matrix/domain specific matrix
- parameter - rule on which parameter to which site type/ matri type?